# odin-recipes
This project is a simple HTML page which is capable of displaying a collection if simple recipes.

This will be entirely build in HTML and is a learning project to teach myself the basics of HTML as I follow along with the TOP foundations course.